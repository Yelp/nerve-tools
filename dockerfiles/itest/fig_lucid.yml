itest:
  build: dockerfiles/itest/itest_lucid
  hostname: itesthost.itestdomain
  volumes:
   - .:/work
  links:
   - locationsuggest
   - geocoder
   - scribe
   - zookeeper

locationsuggest:
  build: dockerfiles/itest/location_suggest

geocoder:
  build: dockerfiles/itest/geocoder

scribe:
  build: dockerfiles/itest/scribe

zookeeper:
  build: dockerfiles/itest/zookeeper
